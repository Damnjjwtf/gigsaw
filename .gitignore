# Environment variables — NEVER commit API keys
.env
.env.*
*.env

# Profile data — keep locally, don't push
profile.json
data/connections.csv

# Output files
output/
builds/*/node_modules/

# System
.DS_Store
node_modules/
*.log

# Sensitive recon data (optional — remove if you want to track)
# data/recon/
# data/evaluations/
